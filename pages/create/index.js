import CreateView from '../../components/CreateView';
import Layout from '../../components/Layout';

const CreatePage = () => {

    return <Layout hidePlaying hideHeader>
        <CreateView />
    </Layout>;

}

export default CreatePage;